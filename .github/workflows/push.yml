name: Provisioning test for Ubuntu

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build the Docker image
        run: docker-compose build ubuntu

      - name: Run provisioning script
        run: docker-compose run ubuntu
